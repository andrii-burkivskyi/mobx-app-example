query GetProjectsList($input: ProjectPaginationInput!){
  data: projects(input: $input){
    items {
        id
        #keys
        name
        color
        url
        #keys
    }
    page
    take
    totalItems
  }
}